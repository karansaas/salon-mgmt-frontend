import { Link } from 'react-router-dom';
import { useAuth } from '../auth/AuthProvider';
import type { Client } from './types';

type ClientTableProps = { clients: Client[]; onDelete(client: Client): void; };

export const ClientTable = ({ clients, onDelete }: ClientTableProps) => { const { user } = useAuth(); const canManage = user?.role !== 'Employee'; return <div className="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table className="min-w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3 font-medium">Client</th><th className="px-5 py-3 font-medium">Mobile</th><th className="px-5 py-3 font-medium">Email</th><th className="px-5 py-3 font-medium">Status</th><th className="px-5 py-3 text-right font-medium">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{clients.map((client) => <tr key={client.id} className="text-slate-700"><td className="whitespace-nowrap px-5 py-4 font-medium text-slate-900">{client.firstName} {client.lastName || ''}</td><td className="whitespace-nowrap px-5 py-4">{client.mobileNumber || '—'}</td><td className="px-5 py-4 text-slate-500">{client.email || '—'}</td><td className="px-5 py-4"><span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${client.isActive ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600'}`}>{client.isActive ? 'Active' : 'Inactive'}</span></td><td className="whitespace-nowrap px-5 py-4 text-right"><Link to={`/clients/${client.id}`} className="mr-3 text-sm font-medium text-slate-600 hover:text-slate-900">View</Link>{canManage && <><Link to={`/clients/${client.id}/edit`} className="mr-3 text-sm font-medium text-brand-600 hover:text-brand-700">Edit</Link><button onClick={() => onDelete(client)} className="text-sm font-medium text-red-600 hover:text-red-700">Delete</button></>}</td></tr>)}</tbody></table></div>; };
