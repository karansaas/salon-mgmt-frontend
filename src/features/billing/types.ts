export type CustomerType = 'EXISTING_CLIENT' | 'WALK_IN';
export type PaymentMethod = 'CASH' | 'UPI' | 'CARD';
export type PaymentStatus = 'PAID' | 'PARTIALLY_PAID' | 'UNPAID';
export type ServiceLine = { serviceId: string; employeeId: string; quantity: number; };
export type ProductLine = { productId: string; quantity: number; };
export type PaymentLine = { method: PaymentMethod; amount: number; };
export type BillRequest = { customerType: CustomerType; clientId?: string; customerName?: string; customerMobile?: string; customerDateOfBirth?: string; services: ServiceLine[]; products: ProductLine[]; discountType: 'FIXED' | 'PERCENTAGE'; discountValue: number; taxRate: number; loyaltyPointsRedeemed?: number; payments: PaymentLine[]; notes?: string; };
export type Bill = { id: string; invoiceNumber: string; client?: string | null; customerType: CustomerType; customerName: string; customerMobile?: string; customerDateOfBirth?: string; customerEmail?: string; services: { serviceId: string; serviceName: string; employeeId: string; employeeName: string; duration: number; unitPrice: number; quantity: number; total: number; }[]; products: { productId: string; productName: string; sku?: string; unitPrice: number; quantity: number; total: number; }[]; subtotal: number; discountType: 'FIXED' | 'PERCENTAGE'; discountValue: number; discountAmount: number; loyaltyPointsRedeemed: number; loyaltyDiscountAmount: number; taxableAmount: number; taxRate: number; taxAmount: number; grandTotal: number; paymentStatus: PaymentStatus; payments: PaymentLine[]; paymentMethods: PaymentMethod[]; notes?: string; createdBy?: { name: string; email: string; role: string; }; createdAt: string; updatedAt: string; };
export type BillListResponse = { bills: Bill[]; pagination: { page: number; limit: number; total: number; totalPages: number; }; };
