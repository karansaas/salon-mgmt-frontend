import { Link } from 'react-router-dom';
import type { Product } from './types';

type Props = { products: Product[]; onDelete(product: Product): void; onAdjust(product: Product): void; basePath?: string; highlightLow?: boolean; professional?: boolean; };
const money = (value: number) => `₹${value.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;

export const ProductTable = ({ products, onDelete, onAdjust, basePath = '/products', highlightLow, professional }: Props) => <div className="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table className="min-w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3 font-medium">Product Name</th><th className="px-5 py-3 font-medium">Category</th><th className="px-5 py-3 font-medium">Brand</th><th className="px-5 py-3 font-medium">{professional ? 'Purchase Price' : 'Selling Price'}</th><th className="px-5 py-3 font-medium">Current Stock</th><th className="px-5 py-3 font-medium">Minimum Stock</th><th className="px-5 py-3 font-medium">Status</th><th className="px-5 py-3 text-right font-medium">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{products.map((product) => { const low = product.currentStock <= product.minimumStockLevel; return <tr key={product.id} className={highlightLow || low ? 'bg-amber-50/70 text-slate-700' : 'text-slate-700'}><td className="whitespace-nowrap px-5 py-4 font-medium text-slate-900">{product.name}</td><td className="whitespace-nowrap px-5 py-4">{product.category}</td><td className="px-5 py-4">{product.brand || '—'}</td><td className="whitespace-nowrap px-5 py-4">{money(professional ? product.purchasePrice : product.sellingPrice)}</td><td className="whitespace-nowrap px-5 py-4"><span>{product.currentStock}</span>{low && <span className="ml-2 rounded-full bg-amber-100 px-2 py-1 text-xs font-semibold text-amber-800">Low stock</span>}</td><td className="whitespace-nowrap px-5 py-4">{product.minimumStockLevel}</td><td className="px-5 py-4"><span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${product.isActive ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600'}`}>{product.isActive ? 'Active' : 'Inactive'}</span></td><td className="whitespace-nowrap px-5 py-4 text-right"><button onClick={() => onAdjust(product)} className="mr-3 text-sm font-medium text-amber-700 hover:text-amber-800">Stock</button><Link to={`${basePath}/${product.id}`} className="mr-3 text-sm font-medium text-slate-600 hover:text-slate-900">View</Link><Link to={`${basePath}/${product.id}/edit`} className="mr-3 text-sm font-medium text-brand-600 hover:text-brand-700">Edit</Link><button onClick={() => onDelete(product)} disabled={!product.isActive} className="text-sm font-medium text-red-600 disabled:opacity-40">Delete</button></td></tr>; })}</tbody></table></div>;
