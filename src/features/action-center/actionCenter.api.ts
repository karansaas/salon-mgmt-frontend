import { api } from '../../lib/api'; export type ActionItem = { id: string; category: string; priority: string; title: string; description: string; reason: string; actionLabel: string; actionRoute: string; opportunity?: boolean; }; export type ActionCenter = { summary: { total: number; high: number; medium: number; low: number; opportunities: number }; actions: ActionItem[]; }; export const getActionCenter = async (): Promise<ActionCenter> => (await api.get<ActionCenter>('/action-center/overview')).data;
