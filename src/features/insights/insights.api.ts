import { api } from '../../lib/api'; export type InsightMetric = { current: number; previous: number; difference: number; percentage: number | null; direction: string; }; export type Insights = { range: { from: string; to: string }; comparisons: Record<string, InsightMetric>; customerInsights: { uniqueCustomers: number; newCustomers: number; returningCustomers: number; inactiveCustomers: number; repeatCustomerRate: number }; services: any[]; products: any[]; employees: any[]; inventory: { lowStock: number; outOfStock: number }; positiveTrends: string[]; attentionRequired: string[]; comparisonAvailable: boolean; }; export const getInsights = async (from: string, to: string): Promise<Insights> => (await api.get<Insights>('/insights/overview', { params: { from, to } })).data;
