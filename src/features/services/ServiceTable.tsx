import { Link } from 'react-router-dom';
import { useAuth } from '../auth/AuthProvider';
import type { Service } from './types';
type Props = { services: Service[]; onDelete(service: Service): void; };
const money = (value: number) => `₹${value.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
export const ServiceTable = ({ services, onDelete }: Props) => { const { user } = useAuth(); const canDelete = user?.role !== 'Employee'; return <div className="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table className="min-w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3 font-medium">Service Name</th><th className="px-5 py-3 font-medium">Category</th><th className="px-5 py-3 font-medium">Price</th><th className="px-5 py-3 font-medium">Status</th><th className="px-5 py-3 text-right font-medium">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{services.map((service) => <tr key={service.id} className="text-slate-700"><td className="whitespace-nowrap px-5 py-4 font-medium text-slate-900">{service.name}</td><td className="whitespace-nowrap px-5 py-4">{service.category}</td><td className="whitespace-nowrap px-5 py-4">{money(service.price)}</td><td className="px-5 py-4"><span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${service.isActive ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600'}`}>{service.isActive ? 'Active' : 'Inactive'}</span></td><td className="whitespace-nowrap px-5 py-4 text-right">{canDelete && <Link to={`/services/${service.id}`} className="mr-3 text-sm font-medium text-slate-600 hover:text-slate-900">View</Link>}<Link to={`/services/${service.id}/edit`} className="mr-3 text-sm font-medium text-brand-600 hover:text-brand-700">Edit</Link>{canDelete && <button onClick={() => onDelete(service)} disabled={!service.isActive} className="text-sm font-medium text-red-600 hover:text-red-700 disabled:cursor-not-allowed disabled:opacity-40">Delete</button>}</td></tr>)}</tbody></table></div>; };
