import axios from 'axios'; import { useMemo, useState } from 'react'; import { useMutation, useQuery } from '@tanstack/react-query'; import { Link } from 'react-router-dom'; import { z } from 'zod'; import { api } from '../lib/api'; import { createBill } from '../features/billing/billing.api'; import type { BillRequest, PaymentLine, ProductLine, ServiceLine } from '../features/billing/types';
type Client = { id: string; firstName: string; lastName?: string; mobileNumber?: string; email?: string; }; type Service = { id: string; name: string; category: string; duration: number; price: number; }; type Product = { id: string; name: string; sku?: string; sellingPrice: number; currentStock: number; isActive: boolean; }; type Employee = { id: string; firstName: string; lastName: string; assignedServices: string[]; isActive: boolean; };
const errorMessage = (error: unknown) => axios.isAxiosError<{ message: string }>(error) ? error.response?.status === 503 ? 'Billing is temporarily unavailable because the database transaction service is not available. Please contact the administrator.' : error.response?.data.message ?? 'Unable to create bill' : 'Unable to create bill'; const money = (v: number) => `₹${v.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`; const round = (v: number) => Math.round((v + Number.EPSILON) * 100) / 100;
const schema = z.object({ customerType: z.enum(['EXISTING_CLIENT', 'WALK_IN']), customerName: z.string().optional(), clientId: z.string().optional(), services: z.array(z.object({ serviceId: z.string().min(1), employeeId: z.string().min(1), quantity: z.number().int().positive() })), products: z.array(z.object({ productId: z.string().min(1), quantity: z.number().int().positive() })), discountValue: z.number().min(0), taxRate: z.number().min(0), payments: z.array(z.object({ method: z.enum(['CASH', 'UPI', 'CARD']), amount: z.number().positive() })) }).superRefine((v, ctx) => { if (!v.services.length && !v.products.length) ctx.addIssue({ code: 'custom', message: 'Add at least one service or product' }); if (v.customerType === 'EXISTING_CLIENT' && !v.clientId) ctx.addIssue({ code: 'custom', message: 'Select an existing client' }); if (v.customerType === 'WALK_IN' && !v.customerName?.trim()) ctx.addIssue({ code: 'custom', message: 'Walk-in customer name is required' }); });
export const NewBillPage = () => { const [customerType, setCustomerType] = useState<'EXISTING_CLIENT' | 'WALK_IN'>('EXISTING_CLIENT'); const [clientSearch, setClientSearch] = useState(''); const [selectedClient, setSelectedClient] = useState<Client | null>(null); const [walkInName, setWalkInName] = useState(''); const [walkInMobile, setWalkInMobile] = useState(''); const [services, setServices] = useState<ServiceLine[]>([]); const [products, setProducts] = useState<ProductLine[]>([]); const [discountType, setDiscountType] = useState<'FIXED' | 'PERCENTAGE'>('FIXED'); const [discountValue, setDiscountValue] = useState(0); const [taxRate, setTaxRate] = useState(0); const [payments, setPayments] = useState<PaymentLine[]>([{ method: 'CASH', amount: 0 }]); const [notes, setNotes] = useState(''); const [validationError, setValidationError] = useState(''); const [created, setCreated] = useState<{ id: string; invoiceNumber: string; grandTotal: number } | null>(null);
  const clientQuery = useQuery({ queryKey: ['billing-clients', clientSearch], queryFn: async () => (await api.get<{ clients: Client[] }>('/clients', { params: { page: 1, limit: 8, search: clientSearch } })).data.clients, enabled: customerType === 'EXISTING_CLIENT' && clientSearch.trim().length >= 2 }); const serviceQuery = useQuery({ queryKey: ['billing-services'], queryFn: async () => (await api.get<{ services: Service[] }>('/services', { params: { page: 1, limit: 100, status: 'active', sortBy: 'name', sortOrder: 'asc' } })).data.services }); const productQuery = useQuery({ queryKey: ['billing-products'], queryFn: async () => (await api.get<{ products: Product[] }>('/products', { params: { page: 1, limit: 100, status: 'active', sortBy: 'name', sortOrder: 'asc' } })).data.products }); const employeeQuery = useQuery({ queryKey: ['billing-employees'], queryFn: async () => (await api.get<{ employees: Employee[] }>('/employees', { params: { page: 1, limit: 100, status: 'active', sortBy: 'firstName', sortOrder: 'asc' } })).data.employees });
  const serviceSubtotal = useMemo(() => round(services.reduce((sum, line) => sum + (serviceQuery.data?.find((s) => s.id === line.serviceId)?.price ?? 0) * line.quantity, 0)), [services, serviceQuery.data]); const productSubtotal = useMemo(() => round(products.reduce((sum, line) => sum + (productQuery.data?.find((p) => p.id === line.productId)?.sellingPrice ?? 0) * line.quantity, 0)), [products, productQuery.data]); const subtotal = round(serviceSubtotal + productSubtotal); const discount = round(Math.min(discountType === 'PERCENTAGE' ? subtotal * discountValue / 100 : discountValue, subtotal)); const taxable = round(subtotal - discount); const tax = round(taxable * taxRate / 100); const grandTotal = round(taxable + tax); const paid = round(payments.reduce((sum, p) => sum + (Number(p.amount) || 0), 0)); const balance = round(grandTotal - paid);
  const mutation = useMutation({ mutationFn: createBill, onSuccess: (bill) => setCreated({ id: bill.id, invoiceNumber: bill.invoiceNumber, grandTotal: bill.grandTotal }) }); const reset = () => { setSelectedClient(null); setClientSearch(''); setWalkInName(''); setWalkInMobile(''); setServices([]); setProducts([]); setDiscountType('FIXED'); setDiscountValue(0); setTaxRate(0); setPayments([{ method: 'CASH', amount: 0 }]); setNotes(''); setValidationError(''); setCreated(null); };
  const submit = () => { setValidationError(''); if (discountType === 'PERCENTAGE' && discountValue > 100) return setValidationError('Percentage discount cannot exceed 100%'); if (discountType === 'FIXED' && discountValue > subtotal) return setValidationError('Fixed discount cannot exceed subtotal'); const duplicateStock = products.reduce<Record<string, number>>((all, line) => ({ ...all, [line.productId]: (all[line.productId] ?? 0) + line.quantity }), {}); const unavailable = Object.entries(duplicateStock).find(([id, qty]) => qty > (productQuery.data?.find((p) => p.id === id)?.currentStock ?? 0)); if (unavailable) return setValidationError('Selected product quantity exceeds available stock'); const payload: BillRequest = { customerType, clientId: selectedClient?.id, customerName: walkInName, customerMobile: walkInMobile, services, products, discountType, discountValue, taxRate, payments: payments.filter((p) => p.amount > 0), notes }; const validated = schema.safeParse(payload); if (!validated.success) return setValidationError(validated.error.issues[0]?.message ?? 'Please complete the bill'); if (paid !== grandTotal) return setValidationError('Payment total must equal the amount due'); mutation.mutate(payload); };
  if (created) return <div className="mx-auto max-w-2xl rounded-xl border border-emerald-200 bg-white p-8 text-center shadow-sm"><p className="text-sm font-semibold text-emerald-700">Invoice created successfully</p><h2 className="mt-2 text-3xl font-bold text-slate-900">{created.invoiceNumber}</h2><p className="mt-2 text-lg text-slate-600">Final amount: {money(created.grandTotal)}</p><div className="mt-7 flex justify-center gap-3"><Link to={`/billing/${created.id}`} className="rounded-lg bg-brand-600 px-5 py-2.5 text-sm font-semibold text-white">View Invoice</Link><button onClick={reset} className="rounded-lg border border-slate-300 px-5 py-2.5 text-sm font-semibold">Create New Bill</button></div></div>;
  const updateService = (index: number, patch: Partial<ServiceLine>) => setServices((rows) => rows.map((row, i) => i === index ? { ...row, ...patch } : row)); const updateProduct = (index: number, patch: Partial<ProductLine>) => setProducts((rows) => rows.map((row, i) => i === index ? { ...row, ...patch } : row));
  return <div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_380px]"><div className="space-y-6"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Customer</h2><div className="mt-4 flex gap-4 text-sm font-medium"><label><input type="radio" checked={customerType === 'EXISTING_CLIENT'} onChange={() => setCustomerType('EXISTING_CLIENT')} /> Existing Client</label><label><input type="radio" checked={customerType === 'WALK_IN'} onChange={() => setCustomerType('WALK_IN')} /> Walk-in Customer</label></div>{customerType === 'EXISTING_CLIENT' ? <div className="mt-4"><input value={clientSearch} onChange={(e) => { setClientSearch(e.target.value); setSelectedClient(null); }} placeholder="Search name or mobile number" className="w-full rounded-lg border border-slate-300 px-3 py-2.5" />{selectedClient ? <div className="mt-3 flex items-center justify-between rounded-lg bg-brand-50 p-3 text-sm"><span><strong>{selectedClient.firstName} {selectedClient.lastName ?? ''}</strong> · {selectedClient.mobileNumber ?? 'No mobile'} {selectedClient.email && `· ${selectedClient.email}`}</span><button onClick={() => setSelectedClient(null)} className="text-brand-700">Change</button></div> : clientQuery.data?.length ? <div className="mt-2 divide-y rounded-lg border">{clientQuery.data.map((client) => <button key={client.id} onClick={() => { setSelectedClient(client); setClientSearch(''); }} className="block w-full px-3 py-2 text-left text-sm hover:bg-slate-50"><strong>{client.firstName} {client.lastName ?? ''}</strong><span className="ml-2 text-slate-500">{client.mobileNumber}</span></button>)}</div> : null}</div> : <div className="mt-4 grid gap-4 sm:grid-cols-2"><input value={walkInName} onChange={(e) => setWalkInName(e.target.value)} placeholder="Customer name *" className="rounded-lg border border-slate-300 px-3 py-2.5" /><input value={walkInMobile} onChange={(e) => setWalkInMobile(e.target.value)} placeholder="Mobile number (optional)" className="rounded-lg border border-slate-300 px-3 py-2.5" /></div>}</section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">Services</h2><button onClick={() => setServices((rows) => [...rows, { serviceId: '', employeeId: '', quantity: 1 }])} className="text-sm font-semibold text-brand-600">+ Add Service</button></div>{services.map((row, index) => { const service = serviceQuery.data?.find((s) => s.id === row.serviceId); const candidates = employeeQuery.data?.filter((e) => e.assignedServices.includes(row.serviceId)) ?? []; return <div key={index} className="mt-4 grid gap-3 rounded-lg bg-slate-50 p-3 md:grid-cols-[2fr_2fr_90px_100px_auto]"><select value={row.serviceId} onChange={(e) => updateService(index, { serviceId: e.target.value, employeeId: '' })} className="rounded-lg border border-slate-300 px-2 py-2"><option value="">Select service</option>{serviceQuery.data?.map((s) => <option key={s.id} value={s.id}>{s.name} · {money(s.price)}</option>)}</select><select value={row.employeeId} onChange={(e) => updateService(index, { employeeId: e.target.value })} disabled={!row.serviceId} className="rounded-lg border border-slate-300 px-2 py-2"><option value="">Select employee</option>{candidates.map((e) => <option key={e.id} value={e.id}>{e.firstName} {e.lastName}</option>)}</select><input value={row.quantity} onChange={(e) => updateService(index, { quantity: Math.max(1, Number(e.target.value)) })} type="number" min="1" className="rounded-lg border border-slate-300 px-2 py-2" /><span className="py-2 text-sm font-medium">{money((service?.price ?? 0) * row.quantity)}</span><button onClick={() => setServices((rows) => rows.filter((_, i) => i !== index))} className="text-sm text-red-600">Remove</button></div>; })}{!services.length && <p className="mt-4 text-sm text-slate-500">No services added.</p>}</section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">Products</h2><button onClick={() => setProducts((rows) => [...rows, { productId: '', quantity: 1 }])} className="text-sm font-semibold text-brand-600">+ Add Product</button></div>{products.map((row, index) => { const product = productQuery.data?.find((p) => p.id === row.productId); const used = products.filter((_, i) => i !== index && products[i].productId === row.productId).reduce((sum, line) => sum + line.quantity, 0); const exceeds = product && row.quantity + used > product.currentStock; return <div key={index} className="mt-4 grid gap-3 rounded-lg bg-slate-50 p-3 md:grid-cols-[2fr_90px_100px_auto]"><div><select value={row.productId} onChange={(e) => updateProduct(index, { productId: e.target.value })} className="w-full rounded-lg border border-slate-300 px-2 py-2"><option value="">Select product</option>{productQuery.data?.map((p) => <option key={p.id} value={p.id}>{p.name}{p.sku ? ` (${p.sku})` : ''} · {money(p.sellingPrice)} · Stock {p.currentStock}</option>)}</select>{exceeds && <p className="mt-1 text-xs text-red-600">Insufficient stock</p>}</div><input value={row.quantity} onChange={(e) => updateProduct(index, { quantity: Math.max(1, Number(e.target.value)) })} type="number" min="1" className="rounded-lg border border-slate-300 px-2 py-2" /><span className="py-2 text-sm font-medium">{money((product?.sellingPrice ?? 0) * row.quantity)}</span><button onClick={() => setProducts((rows) => rows.filter((_, i) => i !== index))} className="text-sm text-red-600">Remove</button></div>; })}{!products.length && <p className="mt-4 text-sm text-slate-500">No products added.</p>}</section></div><aside className="space-y-6"><section className="sticky top-6 rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Bill Summary</h2><div className="mt-4 space-y-2 text-sm"><p className="flex justify-between"><span>Services Subtotal</span><strong>{money(serviceSubtotal)}</strong></p><p className="flex justify-between"><span>Products Subtotal</span><strong>{money(productSubtotal)}</strong></p><p className="flex justify-between border-t pt-2"><span>Subtotal</span><strong>{money(subtotal)}</strong></p></div><div className="mt-4 grid grid-cols-2 gap-2"><select value={discountType} onChange={(e) => setDiscountType(e.target.value as typeof discountType)} className="rounded-lg border border-slate-300 px-2 py-2 text-sm"><option value="FIXED">Fixed discount</option><option value="PERCENTAGE">Percentage</option></select><input value={discountValue} onChange={(e) => setDiscountValue(Math.max(0, Number(e.target.value)))} type="number" min="0" className="rounded-lg border border-slate-300 px-2 py-2" /></div><label className="mt-3 block text-sm font-medium">GST / Tax<select value={taxRate} onChange={(e) => setTaxRate(Number(e.target.value))} className="mt-1 w-full rounded-lg border border-slate-300 px-2 py-2"><option value="0">0%</option><option value="5">5%</option><option value="12">12%</option><option value="18">18%</option><option value="28">28%</option></select></label><div className="mt-4 space-y-2 border-t pt-3 text-sm"><p className="flex justify-between"><span>Discount</span><span>-{money(discount)}</span></p><p className="flex justify-between"><span>Tax</span><span>{money(tax)}</span></p><p className="flex justify-between text-lg font-bold"><span>Grand Total</span><span>{money(grandTotal)}</span></p></div><div className="mt-5 border-t pt-4"><div className="flex items-center justify-between"><h3 className="font-semibold">Payment</h3><button onClick={() => setPayments((rows) => [...rows, { method: 'UPI', amount: 0 }])} className="text-xs font-semibold text-brand-600">+ Mixed payment</button></div>{payments.map((payment, index) => <div key={index} className="mt-2 flex gap-2"><select value={payment.method} onChange={(e) => setPayments((rows) => rows.map((r, i) => i === index ? { ...r, method: e.target.value as PaymentLine['method'] } : r))} className="rounded-lg border border-slate-300 px-2 py-2 text-sm"><option>CASH</option><option>UPI</option><option>CARD</option></select><input value={payment.amount} onChange={(e) => setPayments((rows) => rows.map((r, i) => i === index ? { ...r, amount: Math.max(0, Number(e.target.value)) } : r))} type="number" min="0" step="0.01" className="min-w-0 flex-1 rounded-lg border border-slate-300 px-2 py-2" />{payments.length > 1 && <button onClick={() => setPayments((rows) => rows.filter((_, i) => i !== index))} className="text-red-600">×</button>}</div>)}<div className="mt-3 space-y-1 text-sm"><p className="flex justify-between"><span>Amount Paid</span><strong>{money(paid)}</strong></p><p className={`flex justify-between font-semibold ${balance === 0 ? 'text-emerald-700' : 'text-amber-700'}`}><span>Balance</span><span>{money(balance)}</span></p></div></div><label className="mt-4 block text-sm font-medium">Notes<textarea value={notes} onChange={(e) => setNotes(e.target.value)} rows={2} className="mt-1 w-full rounded-lg border border-slate-300 px-2 py-2" /></label>{validationError && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{validationError}</p>}{mutation.isError && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{errorMessage(mutation.error)}</p>}<button onClick={submit} disabled={mutation.isPending} className="mt-5 w-full rounded-lg bg-brand-600 px-4 py-3 text-sm font-semibold text-white disabled:opacity-60">{mutation.isPending ? 'Creating Bill…' : 'Create Bill'}</button></section></aside></div>; };
