import axios from 'axios'; import { useQuery } from '@tanstack/react-query'; import { Link, useParams } from 'react-router-dom'; import { InformationCard } from '../components/ui/InformationCard'; import { getBill } from '../features/billing/billing.api';
const msg = (e: unknown) => axios.isAxiosError<{ message: string }>(e) ? e.response?.data.message ?? 'Unable to load invoice' : 'Unable to load invoice'; const money = (v: number) => `₹${v.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`; const salon = { name: import.meta.env.VITE_SALON_NAME || 'SalonFlow', address: import.meta.env.VITE_SALON_ADDRESS || 'Your salon address', phone: import.meta.env.VITE_SALON_PHONE || 'Your salon phone', email: import.meta.env.VITE_SALON_EMAIL || 'hello@example.com' };
export const BillDetailsPage = () => { const { id = '' } = useParams(); const query = useQuery({ queryKey: ['bill', id], queryFn: () => getBill(id), enabled: Boolean(id) }); if (query.isLoading) return <p className="text-sm text-slate-500">Loading invoice…</p>; if (query.isError || !query.data) return <div className="rounded-xl bg-red-50 p-5 text-sm text-red-700">{query.isError ? msg(query.error) : 'Invoice not found'}</div>; const bill = query.data; const stamp = new Intl.DateTimeFormat('en-IN', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(bill.createdAt)); return <div className="invoice-print mx-auto max-w-5xl space-y-6"><div className="no-print flex flex-col justify-between gap-3 sm:flex-row"><div><p className="text-sm font-medium text-brand-600">Invoice</p><h2 className="mt-1 text-3xl font-bold">{bill.invoiceNumber}</h2></div><div className="flex gap-3"><button onClick={() => window.print()} className="rounded-lg bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Print Invoice</button><Link to="/billing" className="rounded-lg border border-slate-300 px-4 py-2 text-sm font-medium">Back to Bills</Link></div></div><header className="border-b-2 border-slate-900 pb-5"><div className="flex justify-between gap-4"><div><h1 className="text-2xl font-bold">{salon.name}</h1><p className="mt-1 text-sm text-slate-600">{salon.address}<br />{salon.phone} · {salon.email}</p></div><div className="text-right"><p className="text-sm font-medium text-slate-500">INVOICE</p><p className="text-xl font-bold">{bill.invoiceNumber}</p><p className="mt-1 text-sm text-slate-600">{stamp}</p></div></div></header><div className="grid gap-6 md:grid-cols-2"><InformationCard title="Customer Information"><p className="font-semibold text-slate-900">{bill.customerName}</p><p className="mt-1 text-sm text-slate-600">{bill.customerMobile || 'No mobile number'}</p>{bill.customerEmail && <p className="text-sm text-slate-600">{bill.customerEmail}</p>}{bill.customerType === 'EXISTING_CLIENT' && bill.client && <Link to={`/clients/${bill.client}`} className="no-print mt-3 inline-block text-sm font-medium text-brand-600">View Client Profile</Link>}</InformationCard><InformationCard title="Invoice Information"><dl className="grid grid-cols-2 gap-4 text-sm"><div><dt className="text-slate-400">Payment Status</dt><dd className="mt-1 font-medium">{bill.paymentStatus.replace('_', ' ')}</dd></div><div><dt className="text-slate-400">Created By</dt><dd className="mt-1 font-medium">{bill.createdBy?.name || '—'}</dd></div></dl></InformationCard></div>{bill.services.length > 0 && <InformationCard title="Services"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="border-b text-xs uppercase text-slate-500"><tr><th className="pb-3">Service</th><th className="pb-3">Employee</th><th className="pb-3">Duration</th><th className="pb-3">Quantity</th><th className="pb-3">Price</th><th className="pb-3 text-right">Total</th></tr></thead><tbody className="divide-y">{bill.services.map((line, i) => <tr key={i}><td className="py-3 font-medium">{line.serviceName}</td><td className="py-3">{line.employeeName}</td><td className="py-3">{line.duration} min</td><td className="py-3">{line.quantity}</td><td className="py-3">{money(line.unitPrice)}</td><td className="py-3 text-right">{money(line.total)}</td></tr>)}</tbody></table></div></InformationCard>}{bill.products.length > 0 && <InformationCard title="Products"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="border-b text-xs uppercase text-slate-500"><tr><th className="pb-3">Product</th><th className="pb-3">Quantity</th><th className="pb-3">Price</th><th className="pb-3 text-right">Total</th></tr></thead><tbody className="divide-y">{bill.products.map((line, i) => <tr key={i}><td className="py-3 font-medium">{line.productName}</td><td className="py-3">{line.quantity}</td><td className="py-3">{money(line.unitPrice)}</td><td className="py-3 text-right">{money(line.total)}</td></tr>)}</tbody></table></div></InformationCard>}<div className="grid gap-6 md:grid-cols-[1fr_360px]"><InformationCard title="Payment Information"><div className="space-y-2 text-sm">{bill.payments.length ? bill.payments.map((payment, i) => <p key={i} className="flex justify-between"><span>{payment.method}</span><strong>{money(payment.amount)}</strong></p>) : <p className="text-slate-500">No payment received.</p>}</div></InformationCard><InformationCard title="Summary"><div className="space-y-2 text-sm"><p className="flex justify-between"><span>Subtotal</span><span>{money(bill.subtotal)}</span></p><p className="flex justify-between"><span>Discount</span><span>-{money(bill.discountAmount)}</span></p><p className="flex justify-between"><span>Tax ({bill.taxRate}%)</span><span>{money(bill.taxAmount)}</span></p><p className="flex justify-between border-t pt-3 text-lg font-bold"><span>Grand Total</span><span>{money(bill.grandTotal)}</span></p></div></InformationCard></div>{bill.notes && <InformationCard title="Notes"><p className="text-sm text-slate-700">{bill.notes}</p></InformationCard>}<footer className="border-t pt-5 text-center text-xs text-slate-500">Thank you for choosing {salon.name}. This is a computer-generated invoice.</footer></div>; };
